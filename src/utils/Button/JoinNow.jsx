const JoinNow = ({ data }) => {
  return (
    <div className="JoinNowButton">
      <a href={data} target="_blank" rel="noreferrer noopener">
        Join Now
      </a>
    </div>
  );
};

export default JoinNow;
